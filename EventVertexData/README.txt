This directory contains npz files which each contain 4 numpy arrays saved as npy files.
The 0th array contains event IDs.
The 1st, 2nd, and 3rd arrays contain X, Y, and Z vertex positions respectively.
The arrays are saved such that the Nth event ID corresponds to the Nth instance of vertex data.
